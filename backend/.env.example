# Copy this file to .env and adjust values as needed

# Default (local dev - recommended)
# Uses `backend/docker-compose.yml` (Postgres exposed on localhost:5433)
DATABASE_URL=postgresql://golf:golf@localhost:5433/golfdb

# SQLite fallback
# DATABASE_URL=sqlite:///./golf.db

# Auth0 (optional)
# If AUTH0_DOMAIN and AUTH0_AUDIENCE are set, the API will require a Bearer token.
# AUTH0_DOMAIN=dev-abc123.eu.auth0.com
# AUTH0_AUDIENCE=https://golf-api
# AUTH0_REQUIRED=true
